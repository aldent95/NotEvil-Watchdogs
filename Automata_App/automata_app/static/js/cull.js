var expectedPro;
var minPro;
var acceptedRatio = 1;
var first = true;


function cullFromRatio(parentNode, parentSize){
	if(first){
		var children = 0;
		for (e in parentNode.links) { element_count++; }
		console.log(parentNode.links);
		console.log(children);
		first = false;
	}
}